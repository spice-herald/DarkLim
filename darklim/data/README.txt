This folder is meant to collect small cache files useful to DarkLim: 
so not full data files (those will be stored on tessy or on a cluster) but 
- stored limit curves
- any small response files etc. 
- cached output of darklim (if small enough [<100mb]) 
